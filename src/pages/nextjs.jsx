const NextJsPage = () => {
  return (
    <p>
      Next JS extends React with routing, server-side rendering, and full-stack features
      for production-ready web apps.
    </p>
  )
}

export default NextJsPage
